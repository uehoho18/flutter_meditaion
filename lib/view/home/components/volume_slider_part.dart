import 'package:flutter/material.dart';

class VolimeSliderPart extends StatelessWidget {
  const VolimeSliderPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("PlayButtonsPart"),
      ),
    );
  }
}
