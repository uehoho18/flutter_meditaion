import 'package:flutter/material.dart';

class DecoratedBackgound extends StatelessWidget {
  const DecoratedBackgound({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
