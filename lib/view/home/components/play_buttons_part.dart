import 'package:flutter/material.dart';

class PlayButtonsPart extends StatelessWidget {
  const PlayButtonsPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: const Center(
      child: Text("PlayButtonsPart"),
    ));
  }
}
