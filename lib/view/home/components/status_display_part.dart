import 'package:flutter/material.dart';

class StatusSidPlay extends StatelessWidget {
  const StatusSidPlay({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: const Center(
      child: Text("StatusSidPlay"),
    ));
  }
}
